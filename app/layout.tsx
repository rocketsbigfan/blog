import { ThemeProvider } from "next-themes"
import { siteTitle } from "../components/Layout/index.app"
import '../styles/global.css'
export const metadata = {
  title: siteTitle,
  description: 'Generated by Next.js',
  icons: {
    icon: '/favicon.ico',
  }
}
export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>
        <ThemeProvider
          enableSystem={true}
          attribute="class"
        >
          {/* Layout UI */}
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}